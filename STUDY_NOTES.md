# 学习笔记（接口测试 + 本项目代码走读）

> 面向本人复习 / 面试准备。
> 项目维护看 `HANDOFF.md`，作品展示看 `README.md`，本文件是**学习过程笔记**。

---

## 第 1 讲：接口测试基础 + 框架分层

### 1.1 接口测试是什么
不点界面，直接发 HTTP 请求给后端，校验返回的**状态码**和**响应体**是否符合预期。

### 1.2 HTTP 请求四要素（面试必背）

| 要素 | 说明 | 本项目例子 |
| --- | --- | --- |
| URL | 请求地址 | `api_root_url` + `/users` |
| Method | 请求方法 | GET 查 / POST 增 / PUT 改 / DELETE 删 |
| 参数 | params（拼 URL 上）/ data（表单）/ json（JSON 体） | `rest_client` 的 kwargs |
| Headers | 请求头 | Content-Type、token 等 |

### 1.3 断言（Assertion）
"预期结果 vs 实际结果"的比对。如预期 `code==0` 且 `msg=="查询成功"`，不符则用例失败。

### 1.4 三层分层架构

```
testcases/  用例层：写断言、调关键字
   ↓
operation/  关键字层：把多个接口组合成业务动作
   ↓
api/        接口封装层：把 HTTP 接口封装成 Python 方法（如 list_all_users()）
   ↓
core/       请求封装 + Mock 后端
```

**价值**：后端把 `/users` 改成 `/api/v2/users`，只改 `api/user.py` 一行，用例层零改动。

---

## 核心文件走读

### `core/rest_client.py`（请求封装层）

把发请求封装成 `get/post/put/delete`，用例层不需要关心 requests 怎么写。

**Mock 分支（关键）**：当 `use_mock=True` 时，不走真实网络请求，而是调 `get_api_mock()` 拿到状态码和响应体，塞进一个 `requests.Response()` 对象返回。

**重要概念澄清：**
> `use_mock` **不是**"参数传递类型开关"！

它只是一个**布尔开关**，决定"真发网络请求"还是"用假数据造响应"：

| use_mock | 行为 |
| --- | --- |
| `True` | 走 `get_api_mock()`，用内存假库造响应，**不发网络请求** |
| `False` | 走 `self.session.get/post...`，**真发请求到 `api_root_url`** |

参数类型（params / data / json）在请求前就取好了，**两种模式共用**，和 `use_mock` 无关。

**为什么用例层不用改？**
因为 mock 造的是 `requests.Response()` 对象，和真实请求返回的对象**长得一样**（都有 `.status_code`、`.json()`）。对上层完全透明 —— 这就是 Mock 的价值：**解耦测试与被测服务**。

**踩过的 bug**：原 demo 把 `files` / `cookies` 误用 `params` 取值，已修复为各自取值。

---

### `core/mock_data.py`（状态化契约 Mock）

#### 为什么用"内存假库"而不是"写死返回"？

- ❌ **写死返回**：`调 /register 永远返回 {"code":0,"msg":"注册成功"}`。不管传什么、不管之前注册过没，都成功 → **测不了异常场景**。
- ✅ **内存假库**：`self.users = {...}` 相当于内存里一张"用户表"，能模拟真实业务规则：
  - 注册过的用户名不能再注册（唯一性）
  - 删了的用户再查查不到
  - 登录要用注册时存的密码校验

**一句话**：假库 = 一个"会记住状态的小数据库"，让 mock 能模拟真实的业务规则，而不是无脑返回成功。

#### 业务规则示例（register）

按这个顺序校验：用户名已存在 → `2002`；性别非 0/1 → `2003`；手机号非 11 位数字 → `4008`；都通过才注册成功 → `code:0`。

#### `exec_sql` 状态联动（本项目最巧妙的设计）

**问题**：mock 模式下没有真 MySQL。如果"假 SQL"和"假接口"是两套独立数据，状态就对不上 → 用例逻辑乱掉。

**解法**：让接口操作和数据库操作**共用同一个 `self.users` 字典**。

```
fixture: DELETE FROM user WHERE username='wintest'  →  self.users 删掉 wintest
用例:    register wintest                          →  self.users 加入 wintest
用例:    query wintest                             →  读 self.users，找到了 ✅
```

如果没这联动，第二步加的用户第三步读不到，测试就假绿/假红。

#### 为什么保留 `192.168.89.128`？

这是原作者真实服务地址，`get_api_mock` 把它 strip 掉得到纯路径 `/users` 来做路由。**删了路径就匹配不上 → 用例全挂。这是设计需要，不是残留 bug。**

---

### `api/user.py`（接口封装层）

把 HTTP 接口 `/users` 封装成业务方法 `list_all_users()`。

开关读取：`USE_MOCK = str(_ini["mock"].get("USE_MOCK", "false")).lower() == "true"`，一套代码两种环境。

---

## 第 2 讲：测试用例设计方法（面试必考）

> 核心认知：写用例 = 设计"输入什么 + 预期什么"。用最少用例覆盖最多情况，尤其是不出问题的**边界和异常**。

### 2.1 等价类划分

把输入分成若干"等价类"，**同一类取一个值测，和取全部值效果一样**。

- **有效等价类**：合法输入（应成功）
- **无效等价类**：非法输入（应报错）

例（性别只接受 0/1）：
- 有效：`0`、`1` → 取一个测（如 `1`）
- 无效：`2`、`"男"`、`""` → 取一个代表测（如 `2`）

**价值**：不用把 `2,3,4...` 全测一遍，一个无效值就代表"所有非 0/1 都该报错"。

### 2.2 边界值分析

**错误最爱发生在边界上**。等价类定"测哪类"，边界值定"这类里测哪个点"。

对区间 `[a, b]`：测 `a`、`b`（正好边界）+ `a-1`、`b+1`（刚越界）。

例（手机号 11 位）：测 `10 位` ❌ / `11 位` ✅ / `12 位` ❌

**为什么重要**：很多系统"允许 0~100"写成 `< 100` 漏了 100，或 `<= 100` 多放了 101。边界 1 位之差就是 bug 高发区。

### 2.3 场景法

把"用户怎么用系统"画成流程路径，每条路径 = 一个场景。测**正常流程 + 典型异常流程**。

例（本项目）：注册 → 登录 → 查询 → 修改 → 删除（一条业务线）

### 2.4 本项目两种测试对应

- `testcases/api_test/` = **单接口测试**（等价类 / 边界值为主）
- `testcases/scenario_test/` = **场景测试**（场景法，接口串联）

### 2.5 正向 / 反向用例

- **正向（Positive）**：合法输入，预期成功（如 `code:0`）
- **反向（Negative）**：非法输入，预期报错（如 `code:2002/1004/4008`）

好测试集 = 正向 + 反向都要有。

### 2.6 方法对照表（记这张表）

| 测试方法 | 本项目例子 | 对应 code |
| --- | --- | --- |
| 等价类（无效类） | 性别非 0/1 | `2003` |
| 边界值 | 手机号不是 11 位 | `4008` |
| 等价类（无效类） | 注册已存在的用户名 | `2002` |
| 等价类（无效类） | 查不存在的用户 | `1004` |
| 场景法 | 注册→登录→查询→改→删流程 | `scenario_test` |

---

## 第 3 讲：数据驱动 + 用例落地

### 3.1 YAML = 测试用例数据表

`data/api_test_data.yml` 的 `test_register_user` 段，每行 = 一个用例（8 个字段：5 个输入 + 3 个预期）。

| 行 | 输入 | 预期 | code | 对应方法 |
| --- | --- | --- | --- | --- |
| 1 | 性别 `1` | 成功 | 0 | 有效等价类（正向） |
| 2 | 性别 `3` | 失败 | 2003 | 无效等价类（性别非法） |
| 3 | 用户名已存在 | 失败 | 2002 | 无效等价类（重复注册） |

**这就是等价类落地**：每个无效类取一个代表，不用穷举。

### 3.2 边界值在 update 用例里

- 手机号 `13500010014`（11 位，边界内）→ `code:0` 成功 ✅
- 手机号 `1350001001`（10 位，刚越界）→ `code:4008` 失败 ❌
- 用户 id `111`（不存在）→ `code:4005` 失败（无效等价类）

### 3.3 数据驱动机制

`@pytest.mark.parametrize` 是 pytest 的数据驱动装饰器：

- 第一个参数：8 个变量名（对应 YAML 每行 8 个字段）
- 第二个参数：YAML 里的那 3 行数据
- **效果**：pytest 自动把 3 行变成 **3 个独立用例**分别跑

加一行 YAML = 多一个用例，**代码一行不用改**。这就是数据驱动。

**数据从哪来**：`api_data` 来自 `testcases/conftest.py`（pytest 全局配置文件，所有用例共享），它读了 YAML 文件。

### 3.4 断言怎么写（正向 + 反向统一处理）

4 行断言：

| 断言 | 作用 |
| --- | --- |
| `result.success == except_result` | 业务成功标志是否和预期一致 |
| `status_code == 200` | HTTP 状态码 |
| `code == except_code` | **核心断言**，比对业务返回码 |
| `except_msg in result.msg` | 错误信息**模糊匹配**（用 `in` 容错） |

**注意**：本项目 mock 里**成功和失败 HTTP 状态码都是 200**，错误靠响应体里的 `code` 字段区分 —— 这是很多真实 API 的设计（HTTP 层成功，业务层用 code 表达对错）。

**关键洞察**：正向用例和反向用例**走同一段代码**！区别只是 YAML 里的预期值不同。这就是数据驱动的威力。

### 3.5 Allure 注解：报告为什么好看

- `epic` → `feature` → `story` = 三层目录（在线报告里的分组就是这来的）
- `title` 里用 `{username}` 等变量，让**每个参数化用例有独立标题**（不然 3 个用例都叫"注册"分不清）

### 3.6 整条链路（记这张图）

```
YAML 数据 (api_test_data.yml)
   │  每行 = 一个测试用例（输入 + 预期）
   ▼
@pytest.mark.parametrize   ← 数据驱动，把每行变成独立用例
   ▼
test_02_register.py 的 test_register_user()
   │  调用 register_user()（operation 层）
   ▼
api/user.py → rest_client.py → mock_data.py（发请求 / 造假响应）
   ▼
断言：success / status_code / code / msg
   ▼
Allure 报告（epic/feature/story 分层展示）
```

---

## 自测题（合上文档能答出来 = 真懂了）

1. 接口测试发请求看哪四要素？
2. `use_mock` 到底控制什么？它和参数传递类型有关系吗？
3. Mock 为什么用"内存假库"而不是写死返回？
4. fixture 里 `DELETE FROM user` 在 mock 模式下影响了什么？
5. 等价类和边界值的关系是什么？
6. 手机号 11 位，边界值该测哪几个具体值？
7. `api_test` 和 `scenario_test` 分别对应哪种测试方法？
8. 数据驱动 `@parametrize` 的好处是什么？
9. mock 里成功和失败 HTTP 状态码都是 200，那"失败"靠什么区分？
10. 分层架构（api/operation/testcases）的好处是什么？
